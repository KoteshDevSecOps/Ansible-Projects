- name: demo vars_files
  hosts: local
  connection: local
  vars_files:
    - course.yaml
  tasks:
  - name: print variables informastion
    debug:
      msg: "Hi I am learning {{ COURSE }}, Trainer is {{ TRAINER }}, Duration is {{ DURATION }}"
